= Deploying application via GitOps
include::_attributes.adoc[]

== Deploy your instance of ArgoCD

We will start by deploying an instance of ArgoCD on the cluster. This will be used to deploy the rest of the applications.

Copy the content of the following text, and paste it in the terminal to deploy ArgoCD.

[.lines_space]
[.console-input]

[.lines_space]
[.console-input]

[.lines_space]
[.console-input]
[source, text]
cat <<EOF | oc apply -f -
---
apiVersion: argoproj.io/v1alpha1
kind: ArgoCD
metadata:
  name: argocd
spec:
  sso:
    dex:
      openShiftOAuth: true
      resources:
        limits:
          cpu: 500m
          memory: 256Mi
        requests:
          cpu: 250m
          memory: 128Mi
    provider: dex
  rbac:
    defaultPolicy: "role:readonly"
    policy: "g, system:authenticated, role:admin"
    scopes: "[groups]"
  server:
    insecure: true
    route:
      enabled: true
      tls:
        insecureEdgeTerminationPolicy: Redirect
        termination: edge
EOF

Still in the Terminal, run the following command to check the status of the ArgoCD deployment.

[.lines_space]
[.console-input]
[source, text]
oc rollout status deploy/argocd-server

After a few seconds, the deployment should be complete:

[.bordershadow]
image::05/argocd-rollout.png[]

Now that Argo is deployed, let's find its route.

[.lines_space]
[.console-input]
[source, text]
echo "   ArgoCD UI : https://$(oc get route argocd-server -ojsonpath='{.status.ingress[0].host}')/ "

You should obtain something like this:
  
[.bordershadow]
image::05/argocd-route.png[]

== Deploy the applications via GitOps

Now that ArgoCD is deployed in your namespace, you can deploy the application via GitOps.

[.lines_space]
[.console-input]
[source, text]
cat <<EOF | oc apply -f -
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: claim-insurance-app
spec:
  destination:
    server: https://kubernetes.default.svc
  project: default
  source:
    path: apps/bgd/base
    repoURL: https://github.com/rh-aiservices-bu/insurance-claim-processing.git
    targetRevision: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
    - CreateNamespace=false
EOF


